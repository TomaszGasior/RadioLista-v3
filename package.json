{
    "devDependencies": {
        "@symfony/webpack-encore": "^0.30.0"
    },
    "private": true,
    "scripts": {
        "dev": "encore dev",
        "watch": "encore dev --watch",
        "build": "encore production --progress"
    }
}

twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    date:
        format: 'j F Y'
    form_themes:
        - 'form/fields.html.twig'
